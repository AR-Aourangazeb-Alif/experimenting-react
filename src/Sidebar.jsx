const Sidebar = ({ menuIcon, setMenuIcon }) => {

    return (
        <div className={`h-[100svh] w-44 bg-slate-600 duration-700 ${!menuIcon ? "-ml-60" : ""}`}>

            

        </div>
    );
};

export default Sidebar;